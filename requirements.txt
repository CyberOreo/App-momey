# ── Core (required — installs on all platforms without a compiler) ────────────
-r requirements_core.txt

# ── Live trading only ─────────────────────────────────────────────────────────
# Signs on-chain orders. Only needed when PAPER_TRADING=false.
eth-account>=0.10.0

# ── ML signal scoring (optional — improves win rate over time) ────────────────
scikit-learn>=1.3.0
pandas>=2.1.0

# ── PostgreSQL support (optional — default database is SQLite, no setup) ──────
# Uncomment only if you change DATABASE_URL to postgresql+asyncpg://...
# asyncpg>=0.29.0

# ── Development / testing ─────────────────────────────────────────────────────
pytest>=7.4.0
pytest-asyncio>=0.23.0
